package com.bekmnsrw.inotes

fun main() {
    /*
    ToDo:
    1) BottomSheet with to-do list on DetailsScreen
    2) Trash where notes can be stored for 30 days
        - add attribute isDeleted (null if isn't deleted else time in milliseconds when was deleted)
        - when app was launched should check if 30 days have passed
    3) Replace if with AnimatedVisibility for smooth transitions
    4) TagsScreen:
        - topBar with arrowBack, trashBin and title
        - onTagClicked ()
        - iconDone for selected tagId from previous screen
        - onCardHold
     */
}
